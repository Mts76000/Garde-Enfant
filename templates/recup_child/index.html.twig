{% extends 'base.html.twig' %}

{% block title %}RecupChild index{% endblock %}

{% block body %}
    <h1>RecupChild index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prénom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for recup_child in recup_children %}
            <tr>
                <td>{{ recup_child.id }}</td>
                <td>{{ recup_child.lastName }}</td>
                <td>{{ recup_child.firstName }}</td>
                <td>
                    <a href="{{ path('app_recup_child_show', {'id': recup_child.id}) }}">show</a>
                    <a href="{{ path('app_recup_child_edit', {'id': recup_child.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_recup_child_new') }}">Ajouter</a>
{% endblock %}
