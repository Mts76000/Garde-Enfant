<form method="post" action="{{ path('app_add_creche_delete', {'id': add_creche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ add_creche.id) }}">
    <button class="btn">Delete</button>
</form>
