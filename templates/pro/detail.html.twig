{% extends 'base.html.twig' %}

{% block title %}detail

{% endblock %}

{% block body %}

	<section id="detail" class="bouton">
		<div class="wrap">
			<div class="link">
				<div class="top">
					<a href="{{ path('app_pro_demande') }}">Nouvelle demande</a>
					<a class="message" href="{{ path('app_pro_message') }}">Voir mes message</a>
				</div>
				<div class="down">
					<a class="detail" href="{{ path('app_pro_detail') }}">Détail creche</a>
				<a class="dashboard" href="{{ path('app_pro') }}">Dashboard</a>
				</div>
			</div>
			<div class="creneaux">
				<h2>Ma creche :</h2>
			</div>
			<div class="img">
			<a href="{{ path('app') }}"><img class="back" src="{{ asset('asset/img/back.svg') }}" alt="back"></a>
			</div>
		</div>
	</section>

{% endblock %}
