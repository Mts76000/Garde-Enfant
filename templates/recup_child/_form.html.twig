
{{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
  <div class="recup">
    {{ form_widget(form) }}

    <button class="btn">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
