{% extends 'base.html.twig' %}

{% block title %}dashboard

{% endblock %}

{% block body %}

	<section id="pro">
		<div class="wrap">
			<div class="link">
            <div class="top">
				<a href="{{ path('app_pro_demande') }}">Nouvelle demande</a>
				<a href="{{ path('app_pro_message') }}">Voir mes message</a>
                </div>
                <div class="down">
				<a href="{{ path('app_pro_detail') }}">Détail creche</a>
				<a class="info" href="{{ path('app_pro_info') }}">Mes infos</a>
                </div>
			</div>
            <div class="creneaux">
            <h2>Vos créneaux :</h2>
            </div>
		</div>
	</section>

{% endblock %}
