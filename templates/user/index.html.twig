{% extends 'base.html.twig' %}

{% block title %}mes infos{% endblock %}

{% block body %}


    <section id="user" class="bouton">
        <div class="wrap">
        <div class="select">
            <select name="child" id="child">
                <option value="1">Mathis</option>
                <option value="2">Bilel</option>
            </select>
            </div>
            <div class="link">
                <div class="top">
                    <a  href="{{ path('app_user_rdv') }}">Prendre un rdv</a>
                    <a href="{{ path('app_full_child_index') }}">Voir mes enfants</a>
                </div>
                <div class="down">
                    <a class="info" href="{{ path('app_recup_child_index') }}">Voir les tuteur</a>
                    <a class="dashboard" href="{{ path('app_recup_child_new') }}">Ajouter un tuteur</a>
                </div>
            </div>
            <div class="creneaux">
                <h2>Vos créneaux :</h2>
            </div>
            <div class="img">
                <a href="{{ path('app') }}"><img class="back" src="{{ asset('asset/img/back.svg') }}" alt="back"></a>
            </div>
        </div>
    </section>
{% endblock %}
